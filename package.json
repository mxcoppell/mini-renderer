{
  "name": "mini-render-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/core",
    "dev": "npm run dev --workspace=packages/demo",
    "test": "npm test --workspaces"
  }
}